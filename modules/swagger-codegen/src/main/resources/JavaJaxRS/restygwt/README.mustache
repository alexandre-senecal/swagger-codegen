# Swagger RestyGWT Rest Services generated client

## Overview
This client API was generated by the [swagger-codegen](https://github.com/swagger-api/swagger-codegen) project. By using the 
[OpenAPI-Spec](https://github.com/swagger-api/swagger-core/wiki) from a remote server, you can easily generate a client stub.  This
is an example of building a RestyGWT compatible JAX-RS annotated client.

To use this [RestyGWT](https://resty-gwt.github.io/index.html) client

Inherit the gwt module of your created client

The defaults generate the file as such
```
<inherits name="io.swagger.SwaggerRestyGwtClient"/>
```
Using the config-options you can specify a custom path and module name
```
<inherits name="<gwtModulePackage>.<gwtModuleName>"/>
```

Then simply use your client
```
PetApi service = GWT.create(PetApi.class);
service.addPet(pet, new MethodCallback<Void>() {
    
    public void onSuccess(Method method, Void response) {
      //code your stuff here
    }
    
    public void onFailure(Method method, Throwable exception) {
      //code your stuff here
    });
```